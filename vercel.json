{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm install --no-optional && npm run build",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
